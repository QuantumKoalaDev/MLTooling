module;

#include <mlt/internal/compute/core/MltArray.hpp>

#include <cstddef>

module mlt.compute.Tensor;

using namespace mlt::compute;

Tensor Tensor::from(std::initializer_list<size_t> shape)
{
    Tensor tensor();
    
}
